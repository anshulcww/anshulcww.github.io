var app = angular.module('loginreg', ['ui.router'])

app.controller('MainCtrl', function($scope, $timeout) {
   	
   $scope.name

   
   	/*Two Way Databinding skype?*/
});

  /* $timeout(function(){
         $scope.user.age = 24;
      }, 2000)

      $scope.doLogin = function(){
         console.log($scope.user);
      }*/

/*
	MVC, MVVM
1.	Model - data fetch - from local json file/api request REST API (Representational State Transfer)
2.	View - frontend/html templates
3.	Controller - all logic of the app 
*/
   /*
      var a = favcols;
      for(i = .....){
         el = a[i];
         append.html(el.name)
...      }
   


   javascript -> AngularJS -> Ionicframework (to develop mobile applications apps - )
   iOS apps - objective C, swift
   android - android (java based)
   windows app - vbasic, visual C/c++

   ionic - webbrowser common -  html5/css3/js.. apache cordova + angular -> ionic
   develpoment
   build - .apk , .ios, .vcp
      */